name: load-forecasting
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.26
  - pandas>=2.2
  - pyyaml>=6.0
  - scikit-learn>=1.4
  - pip
  # PyTorch via conda
  - pytorch
  - pip:
    - -e .
